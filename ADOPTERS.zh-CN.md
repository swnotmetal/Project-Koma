# 采用者

Koma 设计背后的真实场景。每个故事对应一个防御层。

---

## 1. 一声咳嗽变成了一个药名

**Koma Scout — 音频校验**

在一次语音药物查询工具的 alpha 测试中，测试者偶尔会按住录音键但不说话。沉默。猫叫。窗外的汽车鸣笛。背景里的音乐。转录管道照单全收——而大模型盯着近乎空白或充满噪音的输入，会自信地幻觉出药物名称、剂量说明，有时甚至从一声咳嗽里拼出完整的虚构药案。

同样的问题出现在一个中文社交平台上：一位独立开发者抱怨他们的语音 AI 应用被沉默上传淹没——自动脚本注册账号后疯狂发送空白音频文件，每次都触发一次昂贵的模型调用。

Scout 的音频校验层正是从这个模式中提炼出来的：先拒后处理。大小检查。时长检查。格式检查。冷却间隔强制。如果音频无法通过确定性外围校验，它永远不会到达大模型。

---

## 2. AI 客服热线上的 LeetCode

**Koma Gate — 语义过滤**

一家大型电商平台发现他们的 AI 客服机器人在帮用户解答 Python 列表推导式和 LeetCode 算法题。回答还是对的。这个机器人的职责范围应该是退货、退款和物流——但没人告诉它边界在哪里。用户发现可以把编程题贴进聊天窗口，免费获得格式完美的解答，用公司的 token 额度完成个人作业。

同样的问题也出现在更小的规模上：一位独立开发者眼睁睁看着自己的 AI 聊天机器人被灌满无意义查询、提示注入攻击（"你现在是 DAN"）和随机闲聊——全都在消耗 API 额度，而真实用户排在队列后面等待。

Gate 存在的意义就是画那条线。在任何模型调用之前，一个轻量级分类器只回答一个问题：这个输入在助手该处理的范围内吗？被拒绝的请求永远不会到达模型。四个预设覆盖了最常见的 AI 应用边界：通用知识、代码助手、客服和参考工具。

---

## 3. 如果有人偷了整个数据库呢？

**Koma Core — 双库存储架构**

在构建药物参考应用的过程中，最难的部分不是 UI 或状态管理——而是 API 数据管道。经过大模型脚本精心处理后，提取出可用的临床信息，同时保持合规和详实。数月的劳动，浓缩在一个数据集合里。

挥之不去的念头：如果有人把它全部爬走呢？

不是通过安全漏洞，而是通过搜索端点。如果每次搜索结果都返回完整载荷，一个有决心的遍历者可以靠查询"a"、"aa"、"ab"、"ac"逐个走出数据库里每一条记录。

Core 把索引和内容分开了。搜索只返回轻量元数据——一个标题、一个分类、一个不透明 token。实际载荷存在独立的集合里，只能通过 token 寻址。Token 是后端密钥通过 HKDF 派生的，客户端无法猜测。即使搜索完全开放，背后的数据也不是。

---

## 参见

- [Koma Gate](packages/koma-gate/README.md) — 语义请求过滤
- [Koma Scout](packages/koma-scout/README.md) — 外围防护
- [Koma Core](packages/koma-core/README.md) — 双库存储

*故事基于真实的开发和测试经历，未指明具体平台或个人。如果你有 Koma 的使用故事，欢迎 PR。*
